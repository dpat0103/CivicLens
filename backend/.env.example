# Defaults to a local SQLite file if unset - no setup required.
# To use Postgres instead:
# DATABASE_URL=postgresql://user:password@localhost:5432/civiclens

# Only needed if you run the real ingestion jobs in ingestion/fetch_census.py
# and ingestion/fetch_bls.py instead of (or in addition to) the seeded sample data.
# CENSUS_API_KEY=
# BLS_API_KEY=
